#include "main.h"
#include <stdio.h>

/**
 * main - Entry function
 * @argc: size of array
 * @argv: Array of arguments
 * Return: Always 0
 */

int main(int argc, char __attribute__((__unused__)) **argv)
{
	printf("%d\n", argc - 1);
	return (0);
}
